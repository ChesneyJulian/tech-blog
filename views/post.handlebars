<div class="container">
{{>nav}}
  <article>
    <h3>{{post.title}}</h3>
    <p>{{post.content}}</p>
  </article>
  <article>
    <h4>Comments</h4>
  {{#each post.comments}}
    <p>Comment by {{username}}: {{content}}</p>
  {{/each}}
  </article>
</div>
